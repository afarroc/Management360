# Mapa de Contexto — App `analyst`

> Generado por `m360_map.sh`  |  2026-03-17 17:35:01
> Ruta: `/data/data/com.termux/files/home/projects/Management360/analyst`  |  Total archivos: **61**

---

## Índice

| # | Categoría | Archivos |
|---|-----------|----------|
| 1 | 👁 `views` | 23 |
| 2 | 🎨 `templates` | 11 |
| 3 | 🗃 `models` | 1 |
| 4 | 📝 `forms` | 1 |
| 5 | ⚙️ `services` | 10 |
| 6 | 🔧 `utils` | 4 |
| 7 | 🔗 `urls` | 1 |
| 8 | 🛡 `admin` | 1 |
| 9 | 🧪 `tests` | 1 |
| 10 | 📄 `other` | 8 |

---

## Archivos por Categoría


### VIEWS (23 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `__init__.py` | 14 | `views/__init__.py` |
| `analyst_base.py` | 782 | `views/analyst_base.py` |
| `clipboard.py` | 227 | `views/clipboard.py` |
| `cross_source.py` | 399 | `views/cross_source.py` |
| `dashboard.py` | 558 | `views/dashboard.py` |
| `data_upload.py` | 1323 | `views/data_upload.py` |
| `__init__.py` | 115 | `views/data_upload_async/__init__.py` |
| `_core.py` | 290 | `views/data_upload_async/_core.py` |
| `clipboard.py` | 113 | `views/data_upload_async/clipboard.py` |
| `dataset.py` | 97 | `views/data_upload_async/dataset.py` |
| `defaults.py` | 470 | `views/data_upload_async/defaults.py` |
| `edit.py` | 260 | `views/data_upload_async/edit.py` |
| `filters.py` | 284 | `views/data_upload_async/filters.py` |
| `upload.py` | 253 | `views/data_upload_async/upload.py` |
| `data_upload_async_old.py` | 1767 | `views/data_upload_async_old.py` |
| `dataset_manager.py` | 354 | `views/dataset_manager.py` |
| `docs.py` | 60 | `views/docs.py` |
| `etl_manager.py` | 863 | `views/etl_manager.py` |
| `excel_analyze.py` | 44 | `views/excel_analyze.py` |
| `file_views.py` | 64 | `views/file_views.py` |
| `other_tools.py` | 96 | `views/other_tools.py` |
| `pipeline.py` | 300 | `views/pipeline.py` |
| `report_builder.py` | 486 | `views/report_builder.py` |

### TEMPLATES (11 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `analyst_base.html` | 2065 | `templates/analyst/analyst_base.html` |
| `cross_source.html` | 1013 | `templates/analyst/cross_source.html` |
| `dashboard.html` | 963 | `templates/analyst/dashboard.html` |
| `dataset_manager.html` | 462 | `templates/analyst/dataset_manager.html` |
| `docs.html` | 165 | `templates/analyst/docs.html` |
| `etl_manager.html` | 1147 | `templates/analyst/etl_manager.html` |
| `load_clipboard_form.html` | 168 | `templates/analyst/load_clipboard_form.html` |
| `clipboard_details.html` | 102 | `templates/analyst/partials/clipboard_details.html` |
| `pipeline.html` | 837 | `templates/analyst/pipeline.html` |
| `report_builder.html` | 752 | `templates/analyst/report_builder.html` |
| `upload_data_csv.html` | 5539 | `templates/analyst/upload_data_csv.html` |

### MODELS (1 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `models.py` | 626 | `models.py` |

### FORMS (1 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `forms.py` | 239 | `forms.py` |

### SERVICES (10 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `base_validator.py` | 351 | `services/base_validator.py` |
| `cross_engine.py` | 429 | `services/cross_engine.py` |
| `data_importer.py` | 196 | `services/data_importer.py` |
| `data_processor.py` | 59 | `services/data_processor.py` |
| `excel_analyzer.py` | 396 | `services/excel_analyzer.py` |
| `excel_processor.py` | 174 | `services/excel_processor.py` |
| `file_processor_service.py` | 342 | `services/file_processor_service.py` |
| `model_mapper.py` | 96 | `services/model_mapper.py` |
| `pipeline_engine.py` | 359 | `services/pipeline_engine.py` |
| `planning.py` | 51 | `services/planning.py` |

### UTILS (4 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `calculations.py` | 33 | `utils/calculations.py` |
| `clipboard.py` | 188 | `utils/clipboard.py` |
| `json_encoder.py` | 0 | `utils/json_encoder.py` |
| `validators.py` | 180 | `utils/validators.py` |

### URLS (1 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `urls.py` | 162 | `urls.py` |

### ADMIN (1 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `admin.py` | 3 | `admin.py` |

### TESTS (1 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `tests.py` | 3 | `tests.py` |

### OTHER (8 archivos)

| Archivo | Líneas | Ruta relativa |
|---------|--------|---------------|
| `ANALYST_CONTEXT.md` | 869 | `ANALYST_CONTEXT.md` |
| `ANALYST_DEV_REFERENCE.md` | 724 | `ANALYST_DEV_REFERENCE.md` |
| `ANALYST_PLATFORM_DESIGN.md` | 430 | `ANALYST_PLATFORM_DESIGN.md` |
| `__init__.py` | 0 | `__init__.py` |
| `apps.py` | 12 | `apps.py` |
| `constants.py` | 45 | `constants.py` |
| `report_functions.py` | 692 | `report_functions.py` |
| `signals.py` | 42 | `signals.py` |

---

## Árbol de Directorios

```
analyst/
├── services
│   ├── base_validator.py
│   ├── cross_engine.py
│   ├── data_importer.py
│   ├── data_processor.py
│   ├── excel_analyzer.py
│   ├── excel_processor.py
│   ├── file_processor_service.py
│   ├── model_mapper.py
│   ├── pipeline_engine.py
│   └── planning.py
├── templates
│   └── analyst
│       ├── partials
│       │   └── clipboard_details.html
│       ├── analyst_base.html
│       ├── cross_source.html
│       ├── dashboard.html
│       ├── dataset_manager.html
│       ├── docs.html
│       ├── etl_manager.html
│       ├── load_clipboard_form.html
│       ├── pipeline.html
│       ├── report_builder.html
│       └── upload_data_csv.html
├── utils
│   ├── calculations.py
│   ├── clipboard.py
│   ├── json_encoder.py
│   └── validators.py
├── views
│   ├── data_upload_async
│   │   ├── __init__.py
│   │   ├── _core.py
│   │   ├── clipboard.py
│   │   ├── dataset.py
│   │   ├── defaults.py
│   │   ├── edit.py
│   │   ├── filters.py
│   │   └── upload.py
│   ├── __init__.py
│   ├── analyst_base.py
│   ├── clipboard.py
│   ├── cross_source.py
│   ├── dashboard.py
│   ├── data_upload.py
│   ├── data_upload_async_old.py
│   ├── dataset_manager.py
│   ├── docs.py
│   ├── etl_manager.py
│   ├── excel_analyze.py
│   ├── file_views.py
│   ├── other_tools.py
│   ├── pipeline.py
│   └── report_builder.py
├── ANALYST_CONTEXT.md
├── ANALYST_DEV_REFERENCE.md
├── ANALYST_PLATFORM_DESIGN.md
├── __init__.py
├── admin.py
├── apps.py
├── constants.py
├── forms.py
├── models.py
├── report_functions.py
├── signals.py
├── tests.py
└── urls.py
```

---

## Endpoints registrados

Fuente: `urls.py`  |  namespace: `analyst`

```python
  path('upload/',                 data_upload.upload_csv,                    name='data_upload'),
  path('upload/preview/',         data_upload_async.preview_async,           name='preview_async'),
  path('upload/preview-page/',    data_upload_async.preview_page_async,      name='preview_page_async'),
  path('upload/confirm/',         data_upload_async.confirm_upload_async,    name='confirm_upload_async'),
  path('edit/delete-columns/',    data_upload_async.delete_columns_async,    name='delete_columns_async'),
  path('edit/replace-values/',    data_upload_async.replace_values_async,    name='replace_values_async'),
  path('edit/fill-na/',           data_upload_async.fill_na_async,           name='fill_na_async'),
  path('edit/convert-date/',      data_upload_async.convert_date_async,      name='convert_date_async'),
  path('edit/rename-column/',     data_upload_async.rename_column_async,     name='rename_column_async'),
  path('edit/drop-duplicates/',   data_upload_async.drop_duplicates_async,   name='drop_duplicates_async'),
  path('edit/sort-data/',         data_upload_async.sort_data_async,         name='sort_data_async'),
  path('edit/convert-dtype/',     data_upload_async.convert_dtype_async,     name='convert_dtype_async'),
  path('edit/normalize-text/',    data_upload_async.normalize_text_async,    name='normalize_text_async'),
  path('edit/filter-count/',      data_upload_async.filter_rows_count_async,  name='filter_rows_count_async'),
  path('edit/filter-delete/',     data_upload_async.filter_rows_delete_async, name='filter_rows_delete_async'),
  path('edit/filter-replace/',    data_upload_async.filter_rows_replace_async, name='filter_rows_replace_async'),
  path('edit/unique-values/',     data_upload_async.filter_unique_values_async, name='filter_unique_values_async'),
  path('edit/apply-defaults/',    data_upload_async.apply_field_defaults_async, name='apply_field_defaults_async'),
  path('clipboard/save-async/',   data_upload_async.save_clipboard_async,    name='save_clipboard_async'),
  path('clipboard/load-preview/', data_upload_async.load_clip_as_preview,    name='load_clip_as_preview'),
  path('upload/save-as-dataset/',  data_upload_async.save_as_dataset,          name='save_as_dataset'),
  path('upload/reanalyze/',           data_upload_async.reanalyze_async,          name='reanalyze_async'),
  path('clipboard/save/',         data_upload.save_clipboard_ajax,           name='save_clipboard_ajax'),
  path('clipboard/load-data/',    data_upload.load_clipboard_data,           name='load_clipboard_data'),
  path('clipboard/details/',          clipboard.clipboard_details,           name='clipboard_details'),
  path('clipboard/export-csv/',       clipboard.export_clipboard_csv,        name='export_clipboard_csv'),
  path('clipboard/list/',             clipboard.clipboard_list,              name='clipboard_list'),
  path('clipboard/load-form/',        clipboard.load_clipboard_form,         name='load_clipboard_form'),
  path('clipboard/load-form-data/',   clipboard.load_clipboard_form_data,    name='load_clipboard_form_data'),
  path('clipboard/delete/',           data_upload.delete_clip_ajax,          name='delete_clip_ajax'),
  path('clipboard/clear-all/',        data_upload.clear_all_clips_ajax,      name='clear_all_clips_ajax'),
  path('datasets/',                       dataset_manager.dataset_list,      name='dataset_list'),
  path('datasets/save/',                  dataset_manager.dataset_save,      name='dataset_save'),
  path('datasets/api/columns/',           dataset_manager.dataset_columns_api, name='dataset_columns_api'),
  path('datasets/<uuid:dataset_id>/delete/',  dataset_manager.dataset_delete,  name='dataset_delete'),
  path('datasets/<uuid:dataset_id>/preview/', dataset_manager.dataset_preview, name='dataset_preview'),
  path('datasets/<uuid:dataset_id>/export/', dataset_manager.dataset_export,   name='dataset_export'),
  path('reports/',                        report_builder.report_list,        name='report_list'),
  path('reports/build/',                  report_builder.report_build,       name='report_build'),
  path('reports/api/functions/',          report_builder.functions_api,      name='functions_api'),
  path('reports/<uuid:report_id>/detail/', report_builder.report_detail,     name='report_detail'),
  path('reports/<uuid:report_id>/delete/', report_builder.report_delete,     name='report_delete'),
  path('reports/<uuid:report_id>/export/', report_builder.report_export,     name='report_export'),
  path('reports/<uuid:report_id>/rerun/',  report_builder.report_rerun,      name='report_rerun'),
  path('file-tree/',          file_views.file_tree_view,                     name='file_tree'),
  path('calculate-agents/',   other_tools.calculate_agents,                  name='calculate_agents'),
  path('traffic-intensity/',  other_tools.calcular_trafico_intensidad_view,  name='traffic_intensity'),
  path('excel/analyze/',      excel_analyze.analyze_excel_ajax,              name='analyze_excel'),
  path('etl/',                             etl_manager.etl_list,             name='etl_list'),
  path('etl/sources/save/',                etl_manager.etl_source_save,      name='etl_source_save'),
  path('etl/sources/<uuid:source_id>/delete/', etl_manager.etl_source_delete, name='etl_source_delete'),
  path('etl/sources/<uuid:source_id>/run/', etl_manager.etl_source_run,      name='etl_source_run'),
  path('etl/jobs/<uuid:job_id>/status/',   etl_manager.etl_job_status,       name='etl_job_status'),
  path('etl/api/models/',                  etl_manager.etl_models_api,       name='etl_models_api'),
  path('etl/api/model-fields/',            etl_manager.etl_model_fields_api, name='etl_model_fields_api'),
  path('bases/',                                     analyst_base.base_list,            name='base_list'),
  path('bases/create/',                              analyst_base.base_create,           name='base_create'),
  path('bases/api/',                                 analyst_base.bases_api,             name='bases_api'),
  path('bases/sources/',                             analyst_base.sources_api,           name='bases_sources_api'),
  path('bases/sources/columns-from-clip/',           analyst_base.clip_columns_api,      name='clip_columns_api'),
  path('bases/<uuid:base_id>/schema/',               analyst_base.base_schema_update,    name='base_schema_update'),
  path('bases/<uuid:base_id>/delete/',               analyst_base.base_delete,           name='base_delete'),
  path('bases/<uuid:base_id>/data/',                 analyst_base.base_data,             name='base_data'),
  path('bases/<uuid:base_id>/rows/add/',             analyst_base.base_row_add,          name='base_row_add'),
  path('bases/<uuid:base_id>/rows/edit/',            analyst_base.base_row_edit,         name='base_row_edit'),
  path('bases/<uuid:base_id>/rows/delete/',          analyst_base.base_row_delete,       name='base_row_delete'),
  path('bases/<uuid:base_id>/bulk-import/',          analyst_base.base_bulk_import,      name='base_bulk_import'),
  path('bases/<uuid:base_id>/bulk-import-raw/',      analyst_base.base_bulk_import_raw,  name='base_bulk_import_raw'),
  path('bases/<uuid:base_id>/export/',               analyst_base.base_export,           name='base_export'),
  path('bases/<uuid:base_id>/columns/',              analyst_base.base_columns_api,      name='base_columns_api'),
  path('cross/',                                cross_source.cross_list,         name='cross_list'),
  path('cross/create/',                         cross_source.cross_create,       name='cross_create'),
  path('cross/api/',                            cross_source.crosses_api,        name='crosses_api'),
  path('cross/api/columns/',                    cross_source.cross_columns_api,  name='cross_columns_api'),
  path('cross/<uuid:cross_id>/update/',         cross_source.cross_update,       name='cross_update'),
  path('cross/<uuid:cross_id>/run/',            cross_source.cross_run,          name='cross_run'),
  path('cross/<uuid:cross_id>/delete/',         cross_source.cross_delete,       name='cross_delete'),
  path('cross/<uuid:cross_id>/preview/',        cross_source.cross_preview,      name='cross_preview'),
  path('dashboards/',                                          dashboard.dashboard_list,        name='dashboard_list'),
  path('dashboards/create/',                                   dashboard.dashboard_create,      name='dashboard_create'),
  path('dashboards/<uuid:dashboard_id>/view/',                 dashboard.dashboard_view,        name='dashboard_view'),
  path('dashboards/<uuid:dashboard_id>/update/',               dashboard.dashboard_update,      name='dashboard_update'),
  path('dashboards/<uuid:dashboard_id>/delete/',               dashboard.dashboard_delete,      name='dashboard_delete'),
  path('dashboards/<uuid:dashboard_id>/layout/save/',          dashboard.dashboard_layout_save, name='dashboard_layout_save'),
  path('dashboards/<uuid:dashboard_id>/widgets/add/',          dashboard.widget_add,            name='widget_add'),
  path('dashboards/<uuid:dashboard_id>/widgets/<uuid:widget_id>/update/', dashboard.widget_update, name='widget_update'),
  path('dashboards/<uuid:dashboard_id>/widgets/<uuid:widget_id>/delete/', dashboard.widget_delete, name='widget_delete'),
  path('dashboards/<uuid:dashboard_id>/widgets/<uuid:widget_id>/data/',   dashboard.widget_data,   name='widget_data'),
  path('pipelines/',                                       pipeline.pipeline_list,   name='pipeline_list'),
  path('pipelines/api/',                                   pipeline.pipeline_api,    name='pipeline_api'),
  path('pipelines/create/',                                pipeline.pipeline_create, name='pipeline_create'),
  path('pipelines/<uuid:pipeline_id>/update/',             pipeline.pipeline_update, name='pipeline_update'),
  path('pipelines/<uuid:pipeline_id>/delete/',             pipeline.pipeline_delete, name='pipeline_delete'),
  path('pipelines/<uuid:pipeline_id>/steps/add/',          pipeline.step_add,        name='pipeline_step_add'),
  path('pipelines/<uuid:pipeline_id>/steps/reorder/',      pipeline.step_reorder,    name='pipeline_step_reorder'),
  path('pipelines/<uuid:pipeline_id>/steps/<int:step_index>/delete/', pipeline.step_delete, name='pipeline_step_delete'),
  path('pipelines/<uuid:pipeline_id>/run/',                pipeline.pipeline_run,    name='pipeline_run'),
  path('pipelines/<uuid:pipeline_id>/runs/',               pipeline.pipeline_runs,   name='pipeline_runs'),
  path('docs/<str:doc_key>/', docs.docs_view, name='docs'),
```

---

## Modelos detectados

**`models.py`**

- línea 12: `class StoredDataset(models.Model):`
- línea 50: `class Report(models.Model):`
- línea 104: `class ETLSource(models.Model):`
- línea 172: `class ETLJob(models.Model):`
- línea 212: `class AnalystBase(models.Model):`
- línea 315: `class CrossSource(models.Model):`
- línea 418: `class Dashboard(models.Model):`
- línea 452: `class DashboardWidget(models.Model):`
- línea 513: `class Pipeline(models.Model):`
- línea 581: `class PipelineRun(models.Model):`


---

## Migraciones

| Archivo | Estado |
|---------|--------|
| `0001_initial` | aplicada |
| `0002_storeddataset_report` | aplicada |
| `0003_alter_report_created_by_alter_report_description_and_more` | aplicada |
| `0004_storeddataset_data_blob` | aplicada |
| `0005_etlsource_etljob` | aplicada |
| `0006_add_analyst_base` | aplicada |
| `0007_remove_etlsource_analyst_base_alter_analystbase_id` | aplicada |
| `0008_etlsource_analyst_base_alter_etlsource_model_path` | aplicada |
| `0009_add_cross_source` | aplicada |
| `0010_alter_crosssource_id` | aplicada |
| `0011_add_dashboard` | aplicada |
| `0012_add_pipeline` | aplicada |
| `0013_etlsource_sim_account` | aplicada |

---

## Funciones clave (views/ y services/)

**`views/analyst_base.py`**

```
48:def _base_row(base: AnalystBase) -> dict:
66:def _paginate(df: pd.DataFrame, page: int, page_size: int) -> dict:
89:def _validate_schema(schema: list) -> list:
139:def base_list(request):
158:def base_create(request):
189:def base_schema_update(request, base_id):
235:def base_delete(request, base_id):
257:def base_data(request, base_id):
288:def base_row_add(request, base_id):
326:def base_row_edit(request, base_id):
361:def base_row_delete(request, base_id):
396:def base_bulk_import(request, base_id):
476:def base_export(request, base_id):
497:def base_columns_api(request, base_id):
512:def bases_api(request):
527:def base_bulk_import_raw(request, base_id):
677:def sources_api(request):
741:def clip_columns_api(request):
```

**`views/clipboard.py`**

```
21:def clipboard_details(request):
71:def export_clipboard_csv(request):
103:def clipboard_list(request):
112:def load_clipboard_form(request):
168:def load_clipboard_form_data(request):
```

**`views/cross_source.py`**

```
41:def _json_safe(obj):
75:def _safe_json_str(obj) -> str:
88:def _cross_row(cs: CrossSource) -> dict:
111:def _validate_config(config: dict) -> str | None:
138:def cross_list(request):
179:def cross_create(request):
210:def cross_update(request, cross_id):
239:def cross_delete(request, cross_id):
260:def cross_run(request, cross_id):
319:def crosses_api(request):
329:def cross_columns_api(request):
355:def cross_preview(request, cross_id):
```

**`views/dashboard.py`**

```
42:def _json_safe(obj):
61:def _safe_json_str(obj) -> str:
72:def _sim_interaction_columns() -> list:
92:def _deserialize(blob: str) -> pd.DataFrame:
96:def _load_df(source: dict, user) -> pd.DataFrame | None:
158:def _compute_widget_data(widget: DashboardWidget, user) -> dict:
273:def _dashboard_row(d: Dashboard) -> dict:
286:def _widget_row(w: DashboardWidget) -> dict:
303:def dashboard_list(request):
375:def dashboard_view(request, dashboard_id):
407:def dashboard_create(request):
428:def dashboard_update(request, dashboard_id):
443:def dashboard_delete(request, dashboard_id):
451:def dashboard_layout_save(request, dashboard_id):
477:def widget_add(request, dashboard_id):
521:def widget_update(request, dashboard_id, widget_id):
542:def widget_delete(request, dashboard_id, widget_id):
554:def widget_data(request, dashboard_id, widget_id):
```

**`views/data_upload.py`**

```
38:def _handle_load_from_clipboard(request):
61:def upload_csv(request):
102:def _init_clipboard(request):
110:def _render_dashboard(request):
136:def _handle_post_actions(request):
203:def _handle_preview(request):
375:def _save_to_session(request, df, cleaned_data, metadata, filename):
406:def _rename_duplicate_columns(df):
431:def _prepare_preview_context(request, form, df, model_fields_info, mapped_columns, required_fields, excel_info):
524:def _handle_confirm_upload(request):
595:def _handle_save_to_clipboard(request):
632:def _handle_delete_clip(request):
651:def _handle_clear_all_clips(request):
664:def _handle_date_conversion(request):
740:def _handle_clipboard_edit(request):
807:def _df_to_session_rows(df):
823:    def _safe(v):
847:def _convert_to_consistent_format(upload_data):
866:def _get_model_fields_info(model):
883:def _map_columns_to_model(request, df, model_fields):
908:def _find_best_match(col_name, available_fields):
941:def _validate_required_fields(model_fields, column_mapping):
959:def save_clipboard_ajax(request):
1019:def load_clipboard_data(request):
1134:def _render_preview_from_session(request):
```

**`views/data_upload_async_old.py`**

```
42:def _serialize(df: pd.DataFrame) -> str:
46:def _deserialize(s: str) -> pd.DataFrame:
50:def _cache_store(key: str, df: pd.DataFrame, meta: dict) -> None:
54:def _cache_load(key: str):
67:def _new_preview_key() -> str:
75:def _field_meta(field) -> dict:
136:def _source_hints(field) -> list[str]:
154:def _analyze(df: pd.DataFrame, model_class):
212:def _preview_json(df: pd.DataFrame, cache_key: str, model_class,
272:def _resolve_model(model_path: str):
287:def preview_async(request):
357:def _edit(request, operation):
401:def delete_columns_async(request):
403:    def op(df, body):
417:def replace_values_async(request):
419:    def op(df, body):
434:def fill_na_async(request):
436:    def op(df, body):
450:def convert_date_async(request):
452:    def op(df, body):
477:def reanalyze_async(request):
528:def save_clipboard_async(request):
580:def confirm_upload_async(request):
650:def load_clip_as_preview(request):
710:def save_as_dataset(request):
```

**`views/dataset_manager.py`**

```
37:def _serialize(df: pd.DataFrame) -> str:
41:def _deserialize(s: str) -> pd.DataFrame:
45:def _load_df(cache_key: str):
57:def _store_df(cache_key: str, df: pd.DataFrame, meta: dict) -> None:
61:def _load_df_persistent(ds: StoredDataset):
97:def _persist_blob(ds: StoredDataset, df: pd.DataFrame) -> None:
109:def _dataset_row(ds: StoredDataset) -> dict:
130:def dataset_list(request):
138:def dataset_save(request):
213:def dataset_delete(request, dataset_id):
227:def dataset_preview(request, dataset_id):
294:def dataset_export(request, dataset_id):
312:def dataset_columns_api(request):
```

**`views/docs.py`**

```
32:def docs_view(request, doc_key):
```

**`views/etl_manager.py`**

```
56:def _get_allowed_apps() -> frozenset:
64:def _is_model_allowed(model_path: str) -> bool:
87:def _validate_sql(sql: str):
98:def _validate_field_name(name: str) -> bool:
102:def _model_field_names(model_path: str) -> set:
132:def _serialize(df: pd.DataFrame) -> str:
136:def _save_as_stored_dataset(df, name, description, source_label, user):
152:def _dtype_for_field(field) -> str:
161:def _discoverable_models() -> list:
178:def _model_fields(model_path: str) -> list:
207:def _source_row(src) -> dict:
226:def _job_row(job) -> dict:
244:def _extract_analyst_base(source, runtime_params, user):
323:def _sim_interaction_fields() -> list:
356:def _extract_sim_account(source, runtime_params, user):
411:def _run_extraction(source, runtime_params, user):
527:def _get_sim_accounts(user) -> list:
545:def etl_list(request):
559:def etl_models_api(request):
593:def etl_model_fields_api(request):
607:def etl_source_save(request):
753:def etl_source_delete(request, source_id):
762:def etl_source_run(request, source_id):
861:def etl_job_status(request, job_id):
```

**`views/excel_analyze.py`**

```
11:def analyze_excel_ajax(request):
```

**`views/file_views.py`**

```
11:def file_tree_view(request):
41:    def get_file_tree(directory):
```

**`views/other_tools.py`**

```
12:def calculate_agents(request):
71:def calcular_trafico_intensidad_view(request):
```

**`views/pipeline.py`**

```
40:def _json_safe(obj):
58:def _safe_json_str(obj) -> str:
69:def _pipeline_row(p: Pipeline) -> dict:
88:def _run_row(r: PipelineRun) -> dict:
110:def pipeline_list(request):
134:def pipeline_api(request):
141:def pipeline_create(request):
163:def pipeline_update(request, pipeline_id):
178:def pipeline_delete(request, pipeline_id):
186:def step_add(request, pipeline_id):
212:def step_delete(request, pipeline_id, step_index):
232:def step_reorder(request, pipeline_id):
253:def pipeline_run(request, pipeline_id):
296:def pipeline_runs(request, pipeline_id):
```

**`views/report_builder.py`**

```
39:def _json_safe(obj):
59:def _safe_json_str(obj) -> str:
74:def _deserialize(s: str) -> pd.DataFrame:
78:def _load_cache(key: str):
89:def _load_source(src: dict, request) -> pd.DataFrame:
154:def _report_row(r: Report) -> dict:
174:def _df_to_json_safe(df: pd.DataFrame) -> list:
195:def report_list(request):
255:def functions_api(request):
262:def report_build(request):
369:def report_detail(request, report_id):
391:def report_delete(request, report_id):
403:def report_export(request, report_id):
420:def report_rerun(request, report_id):
```

**`services/base_validator.py`**

```
44:def _serialize(df: pd.DataFrame) -> str:
48:def _deserialize(blob: str) -> pd.DataFrame:
65:    def _coerce(cls, value: Any, col_def: Dict) -> Tuple[Any, Optional[str]]:
163:    def validate_row(
194:    def validate_dataframe(
236:    def load_dataframe(cls, analyst_base) -> Optional[pd.DataFrame]:
266:    def save_dataframe(cls, analyst_base, df: pd.DataFrame, user) -> None:
322:    def check_unique(
```

**`services/cross_engine.py`**

```
26:def _serialize(df: pd.DataFrame) -> str:
30:def _deserialize(blob: str) -> pd.DataFrame:
38:def load_source(source_desc: dict, request=None, user=None) -> pd.DataFrame:
73:def _load_raw(src_type: str, src_id: str, request, user) -> pd.DataFrame:
87:def _load_stored_dataset(ds_id: str, user) -> pd.DataFrame:
117:def _load_analyst_base(base_id: str, user) -> pd.DataFrame:
132:def _load_etl_source(source_id: str, user) -> pd.DataFrame:
149:def _load_clip(clip_key: str, request) -> pd.DataFrame:
163:def execute_cross(config: dict, request=None, user=None) -> pd.DataFrame:
217:def _do_merge(df_left: pd.DataFrame, df_right: pd.DataFrame,
267:def _do_concat(df_left: pd.DataFrame, df_right: pd.DataFrame,
282:def _apply_filters(df: pd.DataFrame, filters: list) -> pd.DataFrame:
334:def save_cross_result(cross_source, df: pd.DataFrame, user) -> 'StoredDataset':
394:def get_source_columns(source_desc: dict, request=None, user=None) -> list:
```

**`services/data_importer.py`**

```
45:    def _get_model_fields(self) -> Dict:
63:    def import_data(self) -> Dict:
98:    def _validate_import(self):
125:    def _clear_existing_data(self):
134:    def _process_chunk(self, chunk: pd.DataFrame, start_idx: int):
187:    def get_preview_errors(self, max_errors: int = 10) -> List[Dict]:
```

**`services/data_processor.py`**

```
15:    def process_file(file: UploadedFile, model: Model, sheet_name: Optional[str] = None, 
44:    def _process_csv(file: UploadedFile) -> Tuple[pd.DataFrame, Dict]:
```

**`services/excel_analyzer.py`**

```
23:def _col_letter(n):
26:def _range_str(r1, c1, r2, c2):
29:def _cell_has_value(cell):
35:def _build_matrix(ws):
52:def _density(matrix, r1, c1, r2, c2):
58:def _make_region(r1, c1, r2, c2, matrix, idx):
81:def _best_header_fill(matrix, r1, c1, r2, c2, scan_rows=3) -> tuple:
104:def _classify_region(r1, c1, r2, c2, matrix, idx) -> dict:
152:def _find_all_regions(matrix, min_density=0.4, min_cells=2):
167:    def row_groups():
176:    def col_groups(rb_start, rb_end):
185:    def accept(r1, c1, r2, c2):
216:def _find_merge_groups(regions, matrix):
333:    def analyze_sheet(cls, file_obj, sheet_name: str) -> dict:
387:    def get_sheets(cls, file_obj) -> dict:
```

**`services/excel_processor.py`**

```
15:    def process_excel(file, sheet_name=None, cell_range=None, no_header=False):
85:    def _read_with_range(excel_file, sheet_name, cell_range, no_header=False):
126:    def _get_col_letters(cell: str) -> str:
134:    def _get_row_number(cell: str) -> int:
142:    def _col_to_index(col_letters: str) -> int:
152:    def _validate_range(start_col: int, end_col: int, start_row: int, 
167:    def _index_to_col(index: int) -> str:
```

**`services/file_processor_service.py`**

```
19:    def detect_encoding(file) -> str:
38:    def normalize_name(name: str) -> str:
59:    def process_file(cls, file, model: Model, sheet_name: str = None, 
82:    def process_excel(file, sheet_name=None, cell_range=None):
148:    def process_csv(cls, file, model: Model, column_mapping: Dict = None, 
190:    def get_model_fields(cls, model: Model) -> Dict:
202:    def _create_instances_auto(cls, df: pd.DataFrame, model: Model) -> List:
267:    def _create_instances_with_mapping(cls, df: pd.DataFrame, model: Model,
313:    def _convert_value(field, value):
```

**`services/model_mapper.py`**

```
14:    def analyze_model_fields(model: Model, df: pd.DataFrame) -> Tuple[List[Dict], Set[str], List[str]]:
45:    def _get_field_info(field: Field) -> Dict:
56:    def _matches_field(column: str, field_info: Dict) -> bool:
63:    def find_best_match(column_name: str, available_fields: Dict) -> Optional[str]:
92:    def _clean_string(s: str) -> str:
```

**`services/pipeline_engine.py`**

```
31:def _serialize(df: pd.DataFrame) -> str:
34:def _deserialize(s: str) -> pd.DataFrame:
37:def _load_dataset(ds: StoredDataset) -> pd.DataFrame | None:
51:def _save_dataset(df: pd.DataFrame, name: str, user, source_ds: StoredDataset) -> StoredDataset:
75:def _step_delete_columns(df: pd.DataFrame, params: dict):
83:def _step_rename_column(df: pd.DataFrame, params: dict):
94:def _step_replace_values(df: pd.DataFrame, params: dict):
106:def _step_fill_na(df: pd.DataFrame, params: dict):
131:def _step_convert_date(df: pd.DataFrame, params: dict):
147:def _step_convert_dtype(df: pd.DataFrame, params: dict):
172:def _step_normalize_text(df: pd.DataFrame, params: dict):
192:def _step_drop_duplicates(df: pd.DataFrame, params: dict):
201:def _step_sort_data(df: pd.DataFrame, params: dict):
209:def _apply_filter_mask(df: pd.DataFrame, col: str, lookup: str,
226:def _step_filter_delete(df: pd.DataFrame, params: dict):
240:def _step_filter_replace(df: pd.DataFrame, params: dict):
284:    def run(self, output_name: str = None, runtime_params: dict = None) -> tuple:
```

**`services/planning.py`**

```
6:def utilisation(Intensity: float, agents: Union[int, float]) -> float:
10:def top_row(Intensity: float, agents: int) -> float:
14:def bottom_row(Intensity: float, agents: int) -> float:
21:def ErlangC(Intensity: float, agents: int) -> float:
25:def ServiceLevel(Calls: int, Reporting_Period: int, average_handling_time: float, 
32:def ProbCallWaits(Calls: int, Reporting_Period: int, average_handling_time: float, agents: int) -> float:
44:def AgentsFTE(Calls: int, Reporting_Period: int, average_handling_time: float,
```

**`views/data_upload_async/_core.py`**

```
32:def _serialize(df: pd.DataFrame) -> str:
36:def _deserialize(s: str) -> pd.DataFrame:
42:def _cache_store(key: str, df: pd.DataFrame, meta: dict) -> None:
46:def _cache_load(key: str):
59:def _new_preview_key() -> str:
65:def _source_hints(field) -> list:
81:def _field_meta(field) -> dict:
149:def _analyze(df: pd.DataFrame, model_class):
199:def _rows_page(df: pd.DataFrame, page: int, page_size: int) -> tuple:
237:def _preview_json(df: pd.DataFrame, cache_key: str, model_class,
284:def _resolve_model(model_path: str):
```

**`views/data_upload_async/clipboard.py`**

```
22:def save_clipboard_async(request):
66:def load_clip_as_preview(request):
```

**`views/data_upload_async/dataset.py`**

```
23:def save_as_dataset(request):
```

**`views/data_upload_async/defaults.py`**

```
37:def apply_defaults_async(request):
45:    def _slugify(s: str) -> str:
186:def apply_field_defaults_async(request):
196:    def _slugify_str(s):
200:    def _make_unique(base_series, existing_set):
359:def _apply_field_defaults(df: pd.DataFrame, model_class, field_defaults: dict) -> pd.DataFrame:
367:    def _slug(s: str) -> str:
372:    def _initials(first: str, last: str) -> str:
453:def _apply_password_fields(records: list, model_class, field_defaults: dict) -> list:
```

**`views/data_upload_async/edit.py`**

```
31:def _edit(request, operation):
76:def delete_columns_async(request):
78:    def op(df, body):
91:def replace_values_async(request):
93:    def op(df, body):
107:def fill_na_async(request):
109:    def op(df, body):
122:def convert_date_async(request):
124:    def op(df, body):
143:def rename_column_async(request):
145:    def op(df, body):
162:def drop_duplicates_async(request):
164:    def op(df, body):
182:def sort_data_async(request):
184:    def op(df, body):
199:def convert_dtype_async(request):
206:    def op(df, body):
233:def normalize_text_async(request):
237:    def op(df, body):
```

**`views/data_upload_async/filters.py`**

```
30:def _build_mask(df: pd.DataFrame, column: str, operator: str,
85:def filter_rows_count_async(request):
142:def filter_rows_delete_async(request):
144:    def op(df, body):
164:def filter_rows_replace_async(request):
169:    def op(df, body):
227:def filter_unique_values_async(request):
```

**`views/data_upload_async/upload.py`**

```
35:def preview_async(request):
99:def preview_page_async(request):
147:def confirm_upload_async(request):
209:def reanalyze_async(request):
```


---

## Funciones registradas (report_functions.py)

| key | label | category |
|-----|-------|----------|
| `my_fn` | My Label | Category |
| `quality_avg` | Promedio de Notas de Calidad | Calidad |
| `planilla_summary` | Resumen de Planilla | RRHH |
| `period_comparison` | Comparativa entre Períodos | Análisis |
| `ranking` | Ranking por Métrica | Análisis |
| `attendance_report` | Reporte de Asistencia | RRHH |
| `performance_report` | Reporte de Desempeño vs Meta | Análisis |
| `funnel_report` | Embudo de Conversión | Análisis |
| `sim_agent_performance` | Performance por Agente (WFM) | WFM |
| `sim_inbound_daily` | KPIs Inbound Diarios (WFM) | WFM |
| `sim_contact_funnel` | Embudo de Contactabilidad (Outbound) | WFM |

---

## Compartir con Claude

```bash
cat /data/data/com.termux/files/home/projects/Management360/analyst/views/mi_vista.py | termux-clipboard-set
```
